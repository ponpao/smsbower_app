PyQt6>=6.6
PyQt6-Qt6>=6.6
requests>=2.31
keyring>=24.3

# dev / build only — never used to ship a key
python-dotenv>=1.0
pyinstaller>=6.3
Pillow>=10.0   # only needed to regenerate the placeholder app icon
